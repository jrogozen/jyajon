import PostFull from '../../../components/PostFull';

export const meta = {
    title: 'lerna releases with jenkins',
    description: 'CI setup for lerna releases and publishes',
    date: new Date(2020, 9, 18, 4, 15),
    readTime: 30,
    category: 'tools',
}
export default ({ children }) => <PostFull meta={meta}>{children}</PostFull>;

## use case

some stuff.

## creating the hook

ok.