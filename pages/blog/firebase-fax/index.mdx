import PostFull from '../../../components/PostFull';

export const meta = {
    title: 'local firebase faxing webapp',
    description: 'upload and send faxes using twilio and firebase',
    date: new Date(2020, 9, 25, 10, 20),
    readTime: 15,
    category: 'apps',
}

export default ({ children }) => <PostFull meta={meta}>{children}</PostFull>;

## use case

some stuff.

## creating the hook

ok.